// Programme to subtract two numbers
#include<stdio.h>

int main() {
    
    int x, y;

    printf("Enter two numbers ");
    scanf("%d %d", &x, &y);

    printf("Difference of numbers : %d\n", (x-y));

    return 0;
}
