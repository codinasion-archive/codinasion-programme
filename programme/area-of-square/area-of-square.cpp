#include <bits/stdc++.h>
using namespace std;

int main()
{
    double side;
  
    // input 
    cout << "Enter side length: ";
    cin >> side;
  
  double area = side*side;

    // output area
    cout << "Area of Square: " << area;
  
  
  return 0;
}
