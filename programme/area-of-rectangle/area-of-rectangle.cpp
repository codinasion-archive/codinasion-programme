// Write a programme to find the area of Rectangular
#include <bits/stdc++.h>
using namespace std;

int main()
{
    double length, width;
    
    // input radius
    cout << "Enter length: ";
    cin >> length;
  
    cout << "Enter breadth: ";
    cin >> width;

  double result = length* width;
    // output area
    cout << "Area: " << result;
  
  return 0;
}
